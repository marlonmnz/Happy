// Importar dependências (lib)

const express = require('express');

// iniciando o express
const server = express()

// Criar uma rota
server.get('/', () => {
    
})

// Ligar o servidor

server.listen(5500)